# Azure AI Foundry / Azure OpenAI
# Replace placeholders with your own Azure resource/deployment values.
AZURE_AI_PROJECT_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
AZURE_AI_MODEL_DEPLOYMENT_NAME=<your-model-deployment-name>
# Optional: API key for key-based auth (managed identity can be used instead)
# AZURE_OPENAI_API_KEY=replace-me

# Optional: logging format/verbosity
# APP_LOG_LEVEL=INFO
# APP_LOG_FORMAT=json

# Optional: Microsoft Entra ID auth for /v1/* endpoints
# ENTRA_AUTH_ENABLED=false
# ENTRA_TENANT_ID=<tenant-guid>
# ENTRA_AUDIENCE=api://<app-client-id>
# ENTRA_REQUIRED_SCOPES=api.access
# ENTRA_REQUIRED_ROLES=Tutor.Api.Access
# ENTRA_ISSUER=https://login.microsoftonline.com/<tenant-guid>/v2.0
# ENTRA_ISSUERS=https://login.microsoftonline.com/<tenant-guid>/v2.0,https://sts.windows.net/<tenant-guid>/
# ENTRA_TENANT_DOMAIN=<tenant-name>.onmicrosoft.com
# ENTRA_JWKS_URI=https://login.microsoftonline.com/<tenant-guid>/discovery/v2.0/keys
# ENTRA_HTTP_TIMEOUT_SECONDS=5
# ENTRA_JWKS_CACHE_TTL_SECONDS=3600
# FRONTEND_CLIENT_ID=<spa-client-id>
# FRONTEND_AUTHORITY=https://<tenant-name>.ciamlogin.com
# (or AAD tenant authority: https://login.microsoftonline.com/<tenant-guid>)
# FRONTEND_API_SCOPE=api://<api-app-client-id>/api.access
# FRONTEND_IDP_HINT=Google-OAUTH
# FRONTEND_DOMAIN_HINT=Google
# API_RATE_LIMIT_REQUESTS_PER_MINUTE=60
# API_RATE_LIMIT_WINDOW_SECONDS=60

# Optional: MCP connection name (if required by your Foundry environment)
# MCP_PROJECT_CONNECTION_NAME=my-mcp-connection
# Optional: MCP server URL override/fallback (normally resolved from project connection)
# MCP_SERVER_URL=https://learn.microsoft.com/api/mcp

# Optional: PostgreSQL (primary state persistence when configured)
# You can set either POSTGRES_DSN/DATABASE_URL OR discrete POSTGRES_* vars.
# POSTGRES_DSN=postgresql://user:password@host:5432/dbname?sslmode=require
# DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require
# POSTGRES_HOST=pg-server.postgres.database.azure.com
# POSTGRES_PORT=5432
# POSTGRES_DB=mdt
# POSTGRES_USER=mdtadmin
# POSTGRES_PASSWORD=replace-me
# POSTGRES_SSLMODE=require
# STATE_PG_TABLE=student_state

# Optional: Azure Blob persistence (fallback when PostgreSQL is not configured/unavailable)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=...
# AZURE_STORAGE_CONTAINER=mdt-data
# STATE_BLOB_PREFIX=state
# CACHE_BLOB_NAME=cache/cache.json

# Optional: local fallback path for state persistence
# STATE_DIR=.data/state
